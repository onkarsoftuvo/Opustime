<%= t('.To reset your password') %>, <%= t('.click the URL below') %>.

<% #= edit_password_reset_url(@user.password_reset_token) %>
<% path = @domain_path +"/#!/"+ "password_resets/#{@user.password_reset_token}/edit" %>

<%= link_to path , path %>

<%= t('.If you did not request your password to be reset') %>, <%= t('.just ignore this email and your password will continue to stay the same') %>.
